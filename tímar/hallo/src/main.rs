fn main() {
    // u8, u16, u32, u64, u128, usize
    // i8, i16, i32, i64, i128, isize
    // f32, f64
    let tala = 'a'; // 8 bita tala
    //tala /= 10; //  python: 5 // 2
    println!("Hello, world!, talan er: {:x}", tala);
}
